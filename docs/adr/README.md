# Architecture Decision Records (ADRs)

This directory records Tether's architecture decisions in
[MADR](https://adr.github.io/madr/) format. Each ADR captures one decision, its
context, the options weighed, and the consequences, so the *rationale* survives
prose harmonization of the PRD (PRD §12.7).

## How ADRs are created

- New file: `NNNN-kebab-title.md`, copied from [`0000-template.md`](0000-template.md),
  `NNNN` = the next zero-padded integer.
- **Incremental rule (PLAN §0.4 DoD).** A PR that *implements* a still-unhomed
  resolved PRD decision lands that decision's ADR **in the same PR**.
- **M9 gate.** The M9 docs PR fails unless this index is complete (no placeholder
  gaps) — closing PRD §12.7's "home the resolved decisions" deliverable.

## Index

### Foundational, cross-cutting (M0 seed — PLAN M0 S1)

| ADR | Title | Status | PRD anchor |
|----:|-------|--------|------------|
| [0001](0001-provenance-first-data-model.md) | Provenance-first project store | accepted | §3, §5 |
| [0002](0002-smd-superset-round-trip.md) | The `.tether` store is an SMD superset; round-trip is a data-model property | accepted | §5.3, §7.3 |
| [0003](0003-apparent-e-never-nan.md) | Total-correction-failure falls back to apparent-E, never NaN | accepted | §7.2 |
| [0004](0004-pin-and-hold-dual-lock-isolation.md) | Pin-and-hold conda-lock with an isolated tMAVEN sidecar lock | accepted | §4.1, §4.3 |
| [0005](0005-m0-schema-freeze.md) | Freeze the HDF5 schema skeleton at M0 (additive-only thereafter) | accepted | §5, §9 M0 |
| [0006](0006-sidecar-ipc-escalation.md) | Pre-committed sidecar escalation: headless `maven_class`, else bundled IPC | accepted (resolved: headless, IPC not built) | §4.3, §9 M0.5 |
| [0007](0007-parity-is-statistical.md) | Idealization parity is statistical, asserted vs a frozen tolerance | accepted | §7.4, §11.2 |
| [0008](0008-correction-factor-remap.md) | Deep-LASI → Tether correction-factor naming remap (β→α, α→δ, γ→γ) | accepted | Appendix B.1 |

### Homed incrementally by implementing PRs

| ADR | Title | Status | PRD anchor |
|----:|-------|--------|------------|
| [0009](0009-parity-metrics-and-freeze.md) | Idealization-parity metric definitions and the M0.5 freeze | accepted | §7.4, §11.2 |
| [0010](0010-defer-cross-os-gui-handoff.md) | Defer the 2-OS standalone-tMAVEN GUI hand-off check to M9 | accepted | §9 M0.5(a), §9 M9 |
| [0011](0011-home-extraction-recall-at-m1.md) | Home the M0.5 ≥95% extraction-recall acceptance at M1 | accepted | §9 M0.5(b), §9 M1 |
| [0012](0012-registration-pairing-mutual-nn.md) | Registration pairing: mutual NN, fit on original coords; translation prealign first | accepted | App E §7–8, §11.2 |
| [0013](0013-fourier-mellin-similarity-prealign.md) | 4-DOF Fourier-Mellin similarity prealign: log-polar recovery, masked-NCC disambiguation, real-data oracle | accepted | App E §7, §11.2 |
| [0014](0014-registration-map-rms-gate-and-over-gate.md) | Registration map: numeric RMS gate, over-gate flag-don't-drop, unified native/imported calibration | accepted | §7.1, App E §9–10, §11.2 |
| [0015](0015-donor-anchored-colocalization.md) | Donor-anchored colocalization: keep dark/low-FRET acceptors, coordinate-domain apply, both-channel crop box | accepted | §7.1, App E §11–13, §11.2 |
| [0016](0016-extraction-trace-store-layout.md) | Extraction store layout: zero-pad-to-max-T `/traces`, cached patches, the `molecule_key` content hash, the apparent-E substrate | accepted | §5.1, §7.10, App E §14–15, §11.2 |
| [0017](0017-deeplasi-validation-reader.md) | Minimal Deep-LASI `.mat`/`.txt` validation reader: M1-scoped fields, 1-based→0-based coordinates, lazy scipy | accepted | §9 M1, §8 NFR-VALID (a), App A |
| [0018](0018-extract-cli-native-pipeline.md) | `tether extract` CLI: native auto-registration pipeline; imported `.tmap` + Deep-LASI oracle deferred to the S9 follow-up | accepted | §7.11, §9 M1, App E, §11.2 |
| [0019](0019-extract-cli-imported-tmap.md) | `tether extract --tmap`: imported registration path; trust the bead map (residual unknown), refuse a non-identity rotation/flip, defer the apply + oracle | accepted | §7.1, §7.11, §9 M1, App E §6–10 |
| [0020](0020-extraction-oracle-and-deferred-m1-close.md) | The extraction-vs-Deep-LASI acceptance oracle; M1 close deferred (full-scale detection-faithfulness gap surfaced: ~20% recall) | accepted | §9 M1, §8 NFR-VALID (a), §7.11, §2.2, App A |
| [0021](0021-particle-detection-modes.md) | Selectable particle-detection methods (match Deep-LASI's `findPart` modes 1–3) + `.tdat` mode/threshold decode; the deferred PR-C3d framing superseded by 0022 | accepted | §7.1, §9 M1, §11.2, App E |
| [0022](0022-m1-acceptance-reframe-and-close.md) | M1 acceptance reframe (2 px recall, donor-only Pearson ≥0.95, acceptor Pearson diagnostic, faithful per-mode separation, donor-anchored) → **closes M1** (`v0.1.0`) | accepted | §7.1, §9 M1, §8 NFR-VALID (a), §11.2 |
| [0023](0023-curation-label-codec-and-labels-log.md) | Curation-label codec `{UNCURATED 0, ACCEPT +1, REJECT −1}` shared by `curation_label`+`label_value`; append-only `/labels` provenance log with reversible sticky reject + toggleable exclusion; category→`/labels` deferred to M4 | accepted | §5.1, §7.5, §7.3 |
| [0024](0024-idealization-store-layout-staleness-and-nstates.md) | `/idealization/{model}` additive store layout + per-molecule input-provenance hash (staleness) + auto state-count by max-ELBO [Bronson2009]; store-integrated one-click vbFRET (M2 S6 headless core; GUI I-key/overlay is PR-B) | accepted | §5, §7.4, §4.2/§4.3, §11.2 |
| [0025](0025-tmaven-handoff-and-return-leg-reconcile.md) | Bidirectional tMAVEN hand-off SMD export + non-destructive return-leg re-import (exact intensity match, unmatched reported) → new `/idealization/{model}` + per-trace reconcile diff (window re-stales; `class 0↔uncategorized`, class>0 M4-deferred) (M2 S7 headless core; reconcile dialog is PR-B) | accepted | §7.4, §5.3, App D.1 |
| [0026](0026-photobleach-detection-and-window-default.md) | Native single-step photobleach detection (vectorized tMAVEN reimpl, per channel) + summed-intensity analysis-window default into frozen `bleach_frames`/`analysis_window` (manual override wins); `pacc`/`pdon` reframed (constant marker, not per-molecule oracle) → validate vs synthetic known-step + reference parity (opens M3; GUI wiring deferred) | accepted | §7.2, §11.2, App B step 6, App E Stage 16, §9 M3 |
| [0027](0027-leakage-alpha-tail-estimator.md) | Leakage α from the post-acceptor-bleach tail (`ct = mean(I_DA)/mean(I_DD)`, ceiling 0.3, `min_window_frames`/`min_qualifying_traces` gates, median aggregate, withhold-not-fabricate) → frozen `/molecules.alpha` (NaN sentinel) + additive `/settings/leakage` provenance; donor-only-sample global-α + §11.2 band cross-check deferred (committed Cy3-only `.tdat` has `DefaultBeta=0` + traces only in the undecoded MCOS blob → needs an MCOS trace decoder) | accepted | §7.2, §7.4, §11.2, App B.2 step 2, App E Stages 17–18, §9 M3 |
| [0028](0028-gamma-acceptor-bleach-step-estimator.md) | γ across the acceptor-bleach step (`ΔI_A/ΔI_D`, 3-frame half-window levels, both segments `> min_window_frames`, ceiling γ≤5, median aggregate, per-molecule value + median fallback, withhold-not-fabricate) on **bare-`I_D`** leakage-corrected intensities (Tether's additive `I_D,corr=I_D`, not Deep-LASI's `(1+α)`-scaled donor → γ ≈ `(1+α)`× Deep-LASI's) → frozen `/molecules.gamma` (NaN sentinel) + additive `/settings/gamma` provenance; strict estimator-isolated Deep-LASI-median oracle deferred (export's `fret` classification field all-zero + no `.dlss` → no per-frame partition; `median(g)=0.569` recorded) | accepted | §7.2, §7.4, §11.2, App B.2 step 4, App E Stages 17–18, §9 M3 |
| [0029](0029-idealization-correction-provenance-hash-and-per-factor-staleness.md) | Composite corrected-FRET provenance hash (input-trace identity + window bounds + **effective applied** α/γ with the apparent-E toggle folded as `(0,1)`) → per-factor idealization staleness: a γ-median shift re-stales fallback molecules only, a global-α recalibration re-stales the whole cohort; STALE excluded from TDP/dwell (`live_molecule_keys`) + one-click `reidealize`; shared by the fitter and the return-leg importer; additive only (no `/molecules` column, schema-guard green) | accepted | §5.1, §7.2, §7.4, §9 M3 |
| [0030](0030-headless-batch-runner-isolation-and-checkpoint.md) | Headless batch runner (PR7-A): **one `.tether` per movie** (isolation) + **provenance-derived per-stage checkpoint** (`/settings/extraction` · `/settings/correction` · non-empty `/idealization` → resume re-runs only incomplete stages, no new schema); runner-owned correct sequence (photobleach→α→**γ only if α applied**→corrected-E, withheld-α → apparent-E, never NaN); warn/fail over-gate policy reused from ADR-0014; additive `/settings/batch` provenance; `tether batch` CLI; sidecar supervision split to PR7-B | accepted | §6, §7.11, §7.2, §11.2, §9 M3 |
| [0031](0031-batch-sidecar-supervision.md) | Batch sidecar supervision (PR7-B): "liveness" = a one-shot `--probe` startup check (import+instantiate `maven_class`, never raises) → **idealization-deferred mode** (new `deferred` stage status; extract+correct still checkpoint, a later run resumes only idealize) when the sidecar is absent/corrupt; "restart" = re-launch, **auto-restart up to N=3** on a *transient* `SidecarError` (crash/timeout) but **not** on a sidecar-reported fit error (`transient` flag), `RestartsExhausted` fails only that movie; opt-in `SidecarSupervision` owns the interpreter+timeout; CLI supervised-by-default (`--max-restarts`/`--sidecar-timeout`/`--sidecar-python`/`--no-defer`); additive (schema-guard green), new §11.2 tunable | accepted | §7.11, §11.2, §4.3, §9 M3 |
| [0032](0032-nfr-perf-budget-verification.md) | NFR-PERF as a **light M3 gate over envelopes** (not an SLA matrix, §12.10): Qt-free `tether.project.perf` (named budgets + measure/project helpers) + `test_project_perf`. Per-trace render+navigate < `PER_TRACE_LATENCY_BUDGET_S=0.100` (§11.2) timed on a real `TraceDock` (`min_runtime`, ~0.5 ms measured); `.tether` size envelope = the six redundant float32 layers (`measure_store_size` on-disk `/traces` ≤ 1.5× the 24 B/mol/frame float32 model, N-robust; condition projection ≤ 5 GiB); overnight = a slice extraction **pixel-volume-scaled** to the reference 512×512×1700 movie × ~100 movies, fits a 12 h window. Full-movie + real-sidecar SLA deferred to `large-fixtures.yml`; size/overnight kept as derived engineering envelopes (no PRD/§11.2 edit); read-only (schema-guard green) | accepted | §8 NFR-PERF, §11.2, §12.10, §9 M3 |
| [0033](0033-condition-identity-and-rekey.md) | Condition identity = SHA-256 content-hash of the exact key → **keep-separate-by-default** (near-miss strings never auto-merge; homes PR #83's deferred decision) + **transactional re-key** (`rekey_condition`: destination row materialized → **single full-table `/molecules` write** so no molecule is half re-keyed → append-only `/settings/condition_audit` event) with **human-confirmed merge** (`ConfirmationRequired` unless `confirm=True` when the destination already has members; read-only `preview_rekey`). Additive (lazily-created audit dataset, schema-guard green); no §11.2/conda-lock change; GUI dialogs = next M4 PR | accepted | §5.1, §7.6, §9 M4 |
| [0034](0034-gradient-boosting-quality-ranker.md) | Gradient-boosting quality ranker: scikit-learn `HistGradientBoostingClassifier` scoring `P(good)` (native `NaN`, deterministic, `sample_weight`-ready) plugged into `rank_by_score`, ranking by probability of relevance [Robertson1977] as the precision@k objective [Chen2016]; curation-tuned §11.2 defaults (smaller leaves than sklearn stock); single-class/no-label sets refused loudly; never-auto-drop as a permutation invariant; apparent (in-sample) precision@k only — prequential gate, persistence, weighting deferred; additive/read-only (schema-guard green), sklearn import lazy | accepted | §7.5, §11.2, §9 M5 |
| [0035](0035-portable-model-persistence.md) | Portable per-condition model artifact (homes ADR-0034's deferred persistence): a standalone zip (plaintext `manifest.json` validated **before** unpickling `model.pkl`, protocol-5, atomic temp→`os.replace`), `MODEL_FORMAT_VERSION`-gated (a format constant, **not** a §11.2 tunable); **"warm-start-retrain" = honest refit on the accumulated label set** at the video boundary (not sklearn `warm_start` tree-append), carrying `condition_id`/`created_utc` forward + deduped `videos_seen` + `n_retrains`; scikit-learn version mismatch **warns not fails**; trusted-artifacts-only (pickle executes code); store-free/Qt-free — the `/models` reference + owner-curator lock, weighting, prequential gate, seeding, active-learning badge all deferred; no schema/conda-lock change, `import tether.ml` stays sklearn-free | accepted | §7.5, §5.1, §9 M5 |
| [0036](0036-label-weighting-cold-start-decay.md) | Cold-start label weighting (homes ADR-0034/0035's deferred weighting): pure `tether.ml.weighting` decay `w = w₀/(1+n_human)` (`w₀≈0.3`, human `=1.0`; the existing §11.2 tunable, no new one) + thin `tether.project.weighting.recompute_label_weights` rewriting the **frozen-but-§7.5-mutable** `/labels.weight` column in place on each retrain (value-only, schema-guard green); **`n_human` = the condition's human-curated-molecule count** (`/molecules.curation_label != UNCURATED`), the trusted-evidence reading robust to re-curation, not a tally of `/labels` event rows; `train_quality_ranker`/persistence gain an optional `sample_weight` seam (`None` ≡ unweighted); folding provisional/seed rows **into** training deferred to the seeding PR | accepted | §7.5, §5.1, §11.2, §9 M5 |
| [0037](0037-cross-condition-drift-advisory-flag.md) | Cross-condition drift advisory (the **drift-flag** concern of the M5 seeding + multi-curator PR): pure `tether.ml.drift.condition_drift` = a per-engineered-feature two-sample **Kolmogorov–Smirnov** test [Porwik2022] (`method="asymp"`, deterministic) combined with a **Bonferroni** family-wise correction (per-feature threshold α/n_tested; overall α = 0.05, the one new §11.2 tunable) so the matched-condition false-alarm rate stays ≈ α not ≈ 37%; one sweep covers all three §7.5 axes (feature-distribution / `fret_mean`+`fret_var` FRET-range / `snr` SNR); **advisory & overridable**; never fabricates (a `<2`-finite-sample feature is `tested=False` NaN, a wholly-untestable comparison raises); thin store glue `tether.project.drift.cross_condition_drift` groups `/features` by `condition_id`; model-free (no sklearn) + read-only (schema-guard green), no conda-lock change; seeding + multi-curator split-merge deferred | accepted | §7.5, §11.2, §9 M5 |
| [0038](0038-provisional-prior-training-fold.md) | Fold weighted provisional `/labels` priors into the ranker's **training** set (activates ADR-0036's inert `sample_weight` seam): `tether.project.gbranking.weighted_training_set` trains on human labels (full weight) **plus** `deeplasi-provisional`/`cross-condition-seed` priors at their decayed `w₀/(1+n_human)`, with a **human label superseding** a provisional prior on the same molecule (counted once) and the **latest** provisional event per key winning (a provisional `UNCURATED` clears it); weights computed **on the fly** by the same law `recompute_label_weights` persists (extracted into shared `human_counts_by_condition`); **evaluation stays human-only** (apparent precision@k scores `is_good`, raises when no human labels — never a fabricated 0); uncurated-no-prior molecules omitted from training but still ranked; all-unit-weight ⇒ `sample_weight=None` so the human-only fit is unchanged; read-only (schema-guard green), no conda-lock/§11.2 change; seeding *operation* + multi-curator split-merge deferred [Wang2022] | accepted | §7.5, §11.2, §9 M5 |

_Later decisions (the rest of the PRD §12.7 backfill set) are homed incrementally
by the PRs that implement them._
